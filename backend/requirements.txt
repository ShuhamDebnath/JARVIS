# Jarvis — backend dependencies
#
# Version pins are intentional (per ADR-0002 Q6, grilling session 3, 2026-06-01).
# The interpretation task's Pydantic retry behaviour depends on these
# specific versions. Do NOT bump these without re-validating:
#   1. `tests/test_workflow_2_parallelism.py` — async + task_callback
#   2. `contracts/research.py` — output_pydantic + max_retries semantics
#   3. The Q2 trigger matrix — fallback conditions
# Track the corresponding test results in
# backend/output/version_bump_log_{date}.md before changing anything.

# CrewAI — 0.86.0 is the first stable release with `output_pydantic` and
# task-level `max_retries`. 0.95+ adds deprecation warnings for explicit
# `memory=False` (we omit the arg instead — see architecture.md "Per-
# Department Crew Isolation").
#
# Version bump log: 0.86.2 → 0.86.0 on 2026-06-02. 0.86.2 was never
# published to PyPI; the original 0.86.2 pin in this file was a typo.
# See backend/output/version_bump_log_2026-06-02.md for the rationale
# and re-validation plan. Per ADR-0002 Q6.
crewai==0.86.0
crewai-tools==0.17.0

# Pydantic v2 — v1 error messages confuse LLMs ("ensure this value has at
# least 3 items" → "List should have at least 3 items"). v2 is what
# `contracts/research.py` is written against. Pinned to 2.9.2 (latest 2.x
# stable as of 2026-06-02) — must be >= 2.7.0 for crewai 0.86.0's
# instructor dep to resolve.
#
# Version bump log: 2.6.4 → 2.9.2 on 2026-06-02. Original 2.6.4 pin was
# below the floor (2.7.0) that crewai 0.86.0 requires via instructor.
# See backend/output/version_bump_log_2026-06-02.md for the rationale
# and re-validation plan. Per ADR-0002 Q6.
pydantic==2.9.2

# Anthropic SDK — used for the Workflow 1 (Phase 6) Vision LLM only.
# Phase 0a does not require this; install when needed.
# anthropic==0.39.0

# OpenAI SDK — OpenRouter exposes an OpenAI-compatible API.
# Version bump log: 1.30.1 → 1.99.9 on 2026-06-02. Original 1.30.1 pin
# was below the floor (1.40.0) that crewai 0.86.0's litellm dep
# requires transitively. See backend/output/version_bump_log_2026-06-02.md
# for the rationale and re-validation plan. Per ADR-0002 Q6.
openai==1.99.9

# .env loader
python-dotenv==1.0.1

# setuptools — crewai 0.86.0 imports `pkg_resources` at module load time
# (via crewai.telemetry.telemetry). `pkg_resources` was removed from the
# Python stdlib in 3.12 and removed from setuptools in 81.0. Pin to <81
# so `pkg_resources` is still importable. See
# backend/output/version_bump_log_2026-06-02.md.
setuptools>=68.0,<81

# FastAPI — backend HTTP server. Pinned to 0.136.x (the release that
# shipped the modern `lifespan=` kwarg on FastAPI() with full deprecation
# of the old `@app.on_event("startup")` decorators — backend/main.py
# uses lifespan).
#
# Version bump log: 0.136.3 → 0.136.3 on 2026-06-02 (first tracked pin;
# was previously installed transitively as a crewai extra). See
# backend/output/version_bump_log_2026-06-02.md.
fastapi==0.136.3

# Uvicorn — ASGI server that runs `main:app`. Pinned to 0.48.x (the
# release that supports the `[standard]` extras group used in dev).
# Version bump log: 0.48.0 → 0.48.0 on 2026-06-02 (first tracked pin).
# See backend/output/version_bump_log_2026-06-02.md.
uvicorn==0.48.0

# pytest-asyncio — async test runner. Required for the human_gate tests
# in tests/test_human_gate.py (Step 9) which exercise `ask_user()` —
# that function is async. The hello-world crew test (Step 7) is
# synchronous (`crew.kickoff()` is sync in CrewAI 0.86) but we install
# here so the test scaffolding is ready for Step 9.
#
# Pinned to 0.26.0 — the latest 0.x release as of 2026-06-02. The plan
# suggested 0.24.x; 0.26.0 was chosen because it post-dates pytest 9
# (released later in 2025) and is the newest 0.x that still respects
# the "0.x release" locked decision. If a future pytest bump breaks
# this pin, bump to the next 0.x — do NOT jump to 1.x without
# re-checking the plan and tests.
pytest-asyncio==0.26.0

# ----------------------------------------------------------------------------
# Phase 0b — Workflow 2 research tools (installed 2026-06-02)
# ----------------------------------------------------------------------------
# Versions below are first tracked pins. Smoke tests confirm importability
# and (for pytrends + beautifulsoup4) a single demo call. firecrawl-py and
# praw need API keys (FIRECRAWL_API_KEY, REDDIT_CLIENT_ID/SECRET) to run
# live calls — those land in Phase 1 when tools/* wrappers are built.
# Per ADR-0002 Q6 — track any future bumps in
# backend/output/version_bump_log_{date}.md.
firecrawl-py==4.28.2
praw==7.8.1
pytrends==4.9.2
beautifulsoup4==4.14.3

# playwright is installed in 0b.2 (binary browser download is a separate
# step). The Python wheel pulls in pytest-playwright transitively only if
# requested, so we keep the pin explicit.
playwright==1.60.0

# ----------------------------------------------------------------------------
# Phase 3b — Automation (Skyvern + PyAutoGUI)
# ----------------------------------------------------------------------------
# skyvern>=1.0.0 — browser automation for social posting. BROWSER_API_KEY
# required. Falls back to logging-only mode if key not set (graceful deg).
skyvern==1.0.37
# pyautogui>=0.9.54 — cross-platform GUI automation (used as fallback
# when Skyvern is unavailable or unconfigured).
pyautogui==0.9.54

# ----------------------------------------------------------------------------
# Phase 5 — Voice Layer (Wake Word + STT + TTS)  (installed 2026-06-03)
# ----------------------------------------------------------------------------
# Apple Silicon prerequisite: `brew install portaudio` must be present
# BEFORE this pip batch runs. pyaudio 0.2.14 builds against the
# Homebrew-installed `portaudio.h` from /opt/homebrew/include.
# If pyaudio fails with a `portaudio.h not found` compiler error, the
# M1 build-flag override is:
#   uv pip install --python .venv/bin/python --global-option='build_ext' \
#       --global-option='-I/opt/homebrew/include' \
#       --global-option='-L/opt/homebrew/lib' pyaudio
# (per AI-RULES.md Rule 5c + project install notes).

# faster-whisper — local STT. CTranslate2-backed, runs Whisper large-v3
# on Apple Silicon via the arm64 wheel. No API key required.
faster-whisper==1.2.1

# sounddevice — PortAudio bindings for ambient microphone capture.
# Sister library to pyaudio; used because pyaudio's input stream
# ergonomics are awkward in long-lived async loops.
sounddevice==0.5.5

# pyaudio — PortAudio bindings. Required by pvporcupine's Python
# demo helpers and used as the secondary capture path.
pyaudio==0.2.14

# pvporcupine — Picovoice wake-word engine for "Hey Jarvis". Requires
# a free Picovoice AccessKey (https://console.picovoice.ai/) which
# must be added to .env as PICOVOICE_ACCESS_KEY before the engine
# can be initialised. The key is NOT a model download — it is the
# SDK auth token.
pvporcupine==4.0.2

# kokoro-onnx — local ONNX-backed TTS. Synthesises natural speech
# without any cloud API. Models are downloaded on first use from
# the Kokoro ONNX model repo (see Phase 5 implementation notes).
kokoro-onnx==0.5.0

# ctranslate2 — inference engine that faster-whisper depends on for
# CTranslate2-backed Whisper. Pinned explicitly because Apple Silicon
# wheel selection is sensitive to the ctranslate2 build flags.
ctranslate2==4.7.2

# av — PyAV, the audio/video container library used by kokoro-onnx
# for sample-rate / frame-size conversions. Pinned to match the
# version kokoro-onnx 0.5.0 was tested against.
av==17.0.1
